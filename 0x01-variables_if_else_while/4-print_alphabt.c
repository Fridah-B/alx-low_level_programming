#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <conio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Work done)
 */
int main(void)
{
	int y;
	removeChar (q, e)
	for (y = 'A'; y <= 'Z'; y++)
		putchar(tolower(y));
	putchar ('\n');
	return (0);
}
