#include "main.h"
/**
 * _islower - Checks for lowercase letters
 * @c: value entered
 * Return: 0 Always
 */
int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);
	else
		return (0);
	return (0);
}
